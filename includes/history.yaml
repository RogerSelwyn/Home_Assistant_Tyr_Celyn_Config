use_include_order: true
include:
  domains:
    - device_tracker
  entities:
    - binary_sensor.presence_all
    - binary_sensor.presence_cleaner
    - binary_sensor.booking
    - person.clare_selwyn
    - person.roger_selwyn
    - person.rosie_parry
    - binary_sensor.internet
    - binary_sensor.garage_cam
    - binary_sensor.workshop_cam
    - sensor.sky_media_playing
    - sun.sun
    - weather.ty_r_celyn

    - sensor.cpu_temperature
    - sensor.memory_free
    - sensor.processor_use
    - sensor.connected_clients
    
exclude:
  entities:
    - device_tracker.clares_iphone
    - device_tracker.rogers_iphone
    - device_tracker.rosies_phone
    - device_tracker.cleaner_1
